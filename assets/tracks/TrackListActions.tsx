import React from 'react';
import { PlaybackStatus } from '../player/PlaybackTypes';

type TrackListActionsProps = {
    status: PlaybackStatus;
};

const TrackListActions: React.FC<TrackListActionsProps> = () => {
    return <></>;
};

export default TrackListActions;
